<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>cl.zona-ti</groupId>
	<artifactId>gateway-service</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>gateway-service</name>
	<description>Punto unico de entrada: reparte cada request al microservicio que corresponde (compra-service, licitacion-service, y los que se sumen despues).</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<!-- Release train de Spring Cloud compatible con spring-boot-starter-parent
		     4.1.0 de arriba. OJO: el tren "2025.0.x" (versión anterior acá)
		     es para Spring Boot 3.x, con Boot 4 tira un ClassNotFoundException
		     en el arranque (org.springframework.boot.autoconfigure.web.
		     ServerProperties, que Boot 4 reubicó de paquete), porque ese tren
		     nunca conoció la reorganización de paquetes de Boot 4. El tren
		     correcto para Boot 4.x es "2025.1.x" (sus milestones/RC siguieron
		     el desarrollo de Boot 4 en paralelo). Si al compilar Maven no logra
		     resolver las dependencias de este BOM, revisar en
		     https://github.com/spring-cloud/spring-cloud-release/wiki/Supported-Versions
		     cual es el train vigente para esta version de Boot y actualizar. -->
		<spring-cloud.version>2025.1.3</spring-cloud.version>
	</properties>
	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>
	<dependencies>
		<!-- Spring Cloud Gateway (reactivo, sobre WebFlux/Netty). El resto de
		     los microservicios corren sobre el stack clasico (webmvc/Tomcat),
		     pero el gateway es su propio proceso separado, no comparte runtime
		     con ellos, asi que no hay conflicto en usar un stack distinto aca.
		     Desde el tren 2025.0 de Spring Cloud, el artifactId viejo
		     "spring-cloud-starter-gateway" se separo en dos (webflux/webmvc), y
		     quedo obsoleto: el equivalente reactivo nuevo es este de abajo. -->
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-gateway-server-webflux</artifactId>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
